
<h1>困りごと詳細ページ</h1>
<p>タイトル: <%= @problem.title %></p>
<p>画像：
  <% if @problem.image.present? %>
      <%= image_tag @problem.image.url, width: '50%', height: '50%' %>
  <% else %>
    <%= image_tag("noimage.png", :size => "100x100") %>
  <% end %>
</p>

<h3>『解決できるかも！』件数: <%= @problem.likes.count %></h3>
<% if current_user.already_liked?(@problem) %>
  <%= button_to '解決できるかも！を取り消す', problem_like_path(@problem), method: :delete %>
<% else %>
  <%= button_to '解決できるかも！', problem_likes_path(@problem) %>
<% end %>
<h2>『解決できるかも！』したユーザー</h2>
<% @problem.liked_users.each do |user| %>
  <li><%= user.name %></li>
<% end %>


<p>内容: <%= @problem.content %></p>

   <% if @problem.user_id == current_user.id %>
       <td><%= link_to edit_problem_path(@problem.id) do %>
         <div class= " edit btn btn-success" ><span class="glyphicon glyphicon-wrench" aria-hidden="true">困りごとの編集</span></div>
         <% end %>
       </td>
       <td><%= link_to problem_path(@problem.id), method: :delete, data: { confirm: '本当に消去しますか?' } do %>
         <div class= " delete btn btn-danger"><span class="glyphicon glyphicon-wrench" aria-hidden="true">困りごとの消去</span></div>
         <% end %>
       </td>
  <% end %>
<%= link_to 'タスク一覧画面に戻る', problems_path, class: "btn btn-primary" %>
