<%= form_with(model: [problem, comment], url: problem_comments_path(@problem) ) do |f| %>
    <%= f.text_area :content, class: "input-mysize" %><br>
    <%= f.submit "送信", class: "btn btn-outline-dark" %>
<% end %>
