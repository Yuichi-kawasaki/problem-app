<%= form_with(model: @chat,remote:true) do |form| %>
   <%= form.text_field :message, size: "50x3", rows: 5, placeholder: 'メッセージを入力後、ENTERを押してください' %><br>
   <%= form.hidden_field :room_id %>
<% end %>
